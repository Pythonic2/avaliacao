version: '3.8'
services:
  portal:
    build:
      context: .
      dockerfile: Dockerfile
    pull_policy: build
    restart: always
    ports:
      - 8002:8000
    env_file:
      - stack.env
    networks:
      - cloudflare_default
networks:
  cloudflare_default:
    external: true
